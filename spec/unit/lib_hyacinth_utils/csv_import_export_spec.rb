require 'rails_helper'

context 'Hyacinth::Utils::CsvImportExport' do

  before(:all) do
    
  end

  before(:each) do
    
  end

  context ".csv_to_json" do
    it "converts properly" do
      
      expect(1).to eq(1)
    end
  end
  
  context ".json_to_csv" do
    it "converts properly" do
      
      expect(1).to eq(1)
    end
  end

end
