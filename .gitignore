# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config
/.bundle

# Ignore the default SQLite database
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and tempfiles
/log/*.log
/log/resque*
/tmp
# Ignore local space for temporary rake tasks, etc.
/local
/spec/local
# Ignore editor files
*~
\#*\#

# Docker ignores (so these can be customized per-development-environment if needed)
/docker/docker-compose.*.yml
/docker/fedora.*.env

# Ignore config files with sensitive data
master.key
database.yml
datacite.yml
fedora.yml
hyacinth.yml
secrets.yml
solr.yml
image_server.yml
derivative_server.yml
uri_service.yml
term_additional_fields.yml
resque.yml
redis.yml
ezid.yml

*_user_accounts.yml

# subs.yml doesn't need to be stored here because it's not currently related to Hyacinth
config/subs.yml

# Ignore hydra-jetty (no longer used, but we want to make sure that no one accidentally
# commits it now that it's gone, in case they still have a local copy).
/jetty
/jetty-test

# Ignore OS-specific
.DS_Store

.byebug_history
.ruby-gemset
/node_modules

# Ignore coverage reports
/coverage

# Ignore local rake tasks
/lib/tasks/hyacinth/local*.rake

# Vite Ruby
/public/vite*
node_modules
# Vite uses dotenv and suggests to ignore local-only env files. See
# https://vitejs.dev/guide/env-and-mode.html#env-files
*.local
.vite
