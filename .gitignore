# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore uploaded files in development
/storage/*
!/storage/.keep

/node_modules
/yarn-error.log

/public/assets
.byebug_history

# Ignore master key for decrypting credentials and more.
/config/master.key
/public/packs
/public/packs-test
/node_modules
yarn-debug.log*
.yarn-integrity

### Begin Hyacinth Ignores ###

# simplecov coverage report
/coverage

# Ignore config files with sensitive data
database.yml
datacite.yml
hyacinth.yml
lang.yml
redis.yml
resque.yml
secrets.yml
fedora.yml

# Docker ignores (so these can be customized per-development-environment if needed)
/docker/docker-compose.*.yml

# legacy ignores (so they don't accidentally committed if they're still in the workspace)
solr_wrapper.yml
jetty.yml

# ignore any wrapped jetty installs
jetty
jetty-test

# old friends, long forgotten
.DS_Store

# IDE-specific files
/jsconfig.json

# LibreOffice
.~lock*

# Webpack builds
/public/packs
/public/packs-test

# Node and Yarn
/node_modules
/yarn-error.log
yarn-debug.log*
.yarn-integrity

# VSCode
.vscode
